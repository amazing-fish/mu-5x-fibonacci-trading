# Legacy Plans Archive

These documents are retained only for historical context.

They predate the trusted-data generation contract and may reference deleted tracked reports, flat CSV caches, or implementation steps that are no longer current. Use `README.md`, `SKILL.md`, and `docs/architecture.md` as the current entry points.
